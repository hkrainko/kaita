const settings = {
    API_PATH: window.env.API_PATH,
    WS_PATH: window.env.WS_PATH,
    IMG_PATH: window.env.IMG_PATH,
    FILE_PATH: window.env.FILE_PATH,
    IMG_SMALL_SUFFIX: window.env.IMG_SMALL_SUFFIX,
    IMG_MIDDLE_SUFFIX: window.env.IMG_MIDDLE_SUFFIX,
    IMG_LARGE_SUFFIX: window.env.IMG_LARGE_SUFFIX,
}

export default settings
